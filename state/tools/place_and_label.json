{
  "name": "place_and_label",
  "description": "Place a shape at the fixed draw.io drop point, label it, then deselect. Only use when the shape may remain at the default drop point. For multiple free-standing shapes, use place_label_and_move so each shape has an explicit offset.",
  "params": [
    "tool_name",
    "label"
  ],
  "needs_ui_graph": true,
  "steps": [
    {
      "tool": "place_shape",
      "params": {
        "tool_name": "$tool_name"
      }
    },
    {
      "tool": "type_label",
      "params": {
        "text": "$label"
      }
    },
    {
      "tool": "press_escape",
      "params": {}
    },
    {
      "tool": "click_empty_canvas",
      "params": {}
    }
  ]
}
